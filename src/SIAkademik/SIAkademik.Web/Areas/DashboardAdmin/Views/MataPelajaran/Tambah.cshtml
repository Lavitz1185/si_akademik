@using SIAkademik.Web.Areas.DashboardAdmin.Models.MataPelajaranModels
@model TambahVM
@inject IPeminatanRepository PeminatanRepository

@{
    ViewData["Title"] = "Tambah Mata Pelajaran";
    var daftarPeminatan = await PeminatanRepository.GetAll();
}

<!-- Breadcrumb -->
<div class="row" style="justify-content: center;">
    <div class="col-12 col-md-6 col-lg-6" style="padding: 0;">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Manajemen Akademik</a></li>
                <li class="breadcrumb-item"><a asp-area="@AreaNames.DashboardAdmin" asp-controller="MataPelajaran" asp-action="Index">Mata Pelajaran</a></li>
                <li class="breadcrumb-item" aria-current="page">Tambah Data</li>
            </ol>
        </nav>
    </div>
</div>

<!-- Isi Konten -->
<div class="section-body">
    <div class="row" style="justify-content: center;">
        <div class="col-12 col-md-6 col-lg-6">
            <div class="card card-success">
                <div class="card-header" style="padding-top: 20px;">
                    <h4>Tambah Data Mata pelajaran</h4>
                </div>

                <form asp-action="Tambah">
                    <div class="card-body">
                        <div asp-validation-summary="ModelOnly" class="form-text text-danger"></div>

                        <div class="form-group">
                            <label asp-for="@Model.Nama"></label>
                            <input asp-for="@Model.Nama" type="text" class="form-control"/>
                            <span asp-validation-for="@Model.Nama"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="@Model.PeminatanId"></label>
                            <select asp-for="@Model.PeminatanId" class="form-control">
                                @foreach (var peminatan in daftarPeminatan)
                                {
                                    <option value="@peminatan.Id">@peminatan.Nama</option>
                                }
                            </select>
                            <span asp-validation-for="@Model.PeminatanId"></span>
                        </div>
                        
                        <div class="form-group">
                            <label asp-for="@Model.KKM"></label>
                            <input asp-for="@Model.KKM" class="form-control" />
                            <span asp-validation-for="@Model.KKM"></span>
                        </div>                       
                    </div>

                    <div class="card-footer text-right pt-0">
                        <a href="javascript:void(0)" class="btn btn-danger text-white swal-cancel">Batal</a>
                        <button class="btn btn-primary mr-1" type="submit">Simpan</button>
                    </div>                  
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}